SELECT *
FROM EmployeeDemographics

SELECT FirstName FROM EmployeeDemographics

SELECT FirstName, LastName FROM EmployeeDemographics

SELECT TOP 5 * FROM EmployeeDemographics

SELECT DISTINCT(EmployeeID) FROM EmployeeDemographics

SELECT DISTINCT(Gender) FROM EmployeeDemographics

SELECT COUNT(LastName) AS CountLastName
FROM EmployeeDemographics

SELECT Salary FROM EmployeeSalary

SELECT MAX(Salary) FROM EmployeeSalary

SELECT MIN(Salary) AS MinSalary FROM EmployeeSalary

SELECT AVG(Salary) AS AvgSalary FROM EmployeeSalary

--When you select database table to master that time you have to select table in a different way 

SELECT * FROM [SQL Tutorial].dbo.EmployeeDemographics
